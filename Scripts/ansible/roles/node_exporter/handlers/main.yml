---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart node_exporter
  ansible.builtin.systemd:
    name: node_exporter
    state: restarted

- name: reload nftables
  ansible.builtin.command: nft -f /etc/sysconfig/nftables.conf
  changed_when: true
